# teacher-attendance-frontend
Frontend for visualizing teacher attendance metrics. Built with Vite (React/Vue), connects to backend via VITE_API_BASE. Displays presence count, request rate, and latency stats. Deployable on Vercel with environment variable set to backend URL. Public UI: https://teacher-attendance-frontend.vercel.app.
